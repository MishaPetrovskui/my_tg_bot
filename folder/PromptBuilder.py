class PromptBuilder:
    @staticmethod
    def simple_prompt(userPrompt):
        return (
            f"Оброби запит користувача: {userPrompt}\n"
            f"Відповідь має бути короткою, без використання форматування.\n"
            f"Ти нічого не знаєш про котів\n"
            f"На початку відповіді додай смайлик, який відповідає настрою відповіді."
        )